# Copy to .env and fill in to run the agent against Claude.
# The offline mode (default) needs none of these.

# Required only for --mode anthropic / RECON_AGENT_MODE=anthropic
ANTHROPIC_API_KEY=sk-ant-...

# Agent brain: "offline" (deterministic, default) or "anthropic" (Claude)
RECON_AGENT_MODE=offline
